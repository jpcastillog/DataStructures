#include <iostream>

template<typename T, typename Type_p>
void partition(const T begin, const T end, size_t p){
    
    return;
};

template<typename T>
void quickSort(const T begin, const T end){
    return;
};

template<typename T>
void merge(const T beginLeft, const T endLeft, 
            const T beginRight, const T endRight, 
           const T beginAux){
        
    return;
};

template<typename T>
void mergeSort(const T begin, const T end){
    return;
};


template<typename T>
void heapSort(const T begin, const T end){
    return;
}